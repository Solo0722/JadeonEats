import { useContext } from "react";
import { AppContext } from "../context/Context";

export const LocationRequest = () => {
  const { setDeliveryAddress, deliveryAddress } = useContext(AppContext);

  
};
